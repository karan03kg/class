<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Styled Header</title>
    <style>
        /* Header styles */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            background-color: #333;
            color: #fff;
        }

        /* Website name styling */
        header h3 {
            margin: 0;
            font-size: 1.5rem;
        }

        /* Navigation styling */
        nav a {
            color: #fff;
            text-decoration: none;
            margin-left: 1rem;
            font-size: 1rem;
        }

        nav a:hover {
            text-decoration: underline;
        }

        /* Button styling */
        button {
            background-color: #f44336;
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #d32f2f;
        }

        /* Removing the default link styling inside the button */
        button a {
            color: inherit;
            text-decoration: none;
        }
    </style>
</head>
<body>

<header>
    <h3>Website Name</h3>
    <nav>
        <a href="home">Home</a>
    </nav>
    <button><a href="Home">Logout</a></button>
</header>

</body>
</html>
